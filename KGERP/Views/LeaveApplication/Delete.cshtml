@model KGERP.Service.ServiceModel.LeaveApplicationModel

@{
    ViewBag.Title = "Delete";
}



<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Leave Application</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.ManagerId)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Employee.ManagerId)
        </dd>

        <dt>

            @Html.DisplayNameFor(model => model.ApplicationDate)
        </dt>

        <dd>
            @if (Model.ApplicationDate.HasValue)
            {
                @Model.ApplicationDate.Value.ToString("dd MMM yyyy")
            }
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.LeaveCategory)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.LeaveCategory.Name)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.StartDate)
        </dt>

        <dd>
            @if (Model.StartDate.HasValue)
            {
                @Model.StartDate.Value.ToString("dd MMM yyyy")
            }
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.EndDate)
        </dt>

        <dd>
            @if (Model.EndDate.HasValue)
            {
                @Model.EndDate.Value.ToString("dd MMM yyyy")
            }
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.LeaveDays)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.LeaveDays)
        </dd>
    </dl>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-danger" /> |
            @Html.ActionLink("Back to List", "Index")
        </div>
    }
</div>

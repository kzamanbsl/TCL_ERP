@model KGERP.ViewModel.VendorViewModel
@{
    ViewBag.Title = "Customer Receiables";
}


<div class="row">
    @using (Html.BeginForm("Index", "Vendor", FormMethod.Get))
    {
        <div class="col-md-6">
            @Html.Label("Customer Payment List", new { @style = "font-size:25px" })
        </div>
        <div class="col-md-6">
            @*<div class="col-md-8">@Html.TextBox("searchText", null, new { @autocomplete = "off", @class = "form-control pull-left", @placeholder = "Search..." })</div>
                <div class="col-md-2">
                    <button type="submit" class="btn btn-success pull-left">Search&nbsp;<i class="fa fa-search"></i></button>
                </div>*@

            <a href="@Url.Action("Index")" class="btn btn-primary pull-right">
                <span class="glyphicon glyphicon-arrow-left"></span> Back
            </a>

        </div>
    }
</div>
<div class="row">
    <table class="table">
        <tr>
            <td><b>Customer Name</b></td>
            <td>@Html.DisplayFor(model => model.Vendor.Name)</td>
            <td><b>@Html.DisplayNameFor(model => model.Vendor.Address)</b></td>
            <td>@Html.DisplayFor(model => model.Vendor.Address)</td>
        </tr>
        <tr>
            <td><b>@Html.DisplayNameFor(model => model.Vendor.Phone)</b></td>
            <td>@Html.DisplayFor(model => model.Vendor.Phone)</td>
            <td><b>@Html.DisplayNameFor(model => model.Vendor.PaymentDue)</b></td>
            <td>@Html.DisplayFor(model => model.Vendor.PaymentDue)</td>
        </tr>
    </table>
</div>

<div class="row">

    <table class="table table-striped table-responsive">
        <tr style="background-color:#009270;color:#FFFFFF">
            <th>SL</th>
            <th>
                @Html.DisplayNameFor(model => model.Payments.First().PaymentDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Payments.First().PaymentMethod)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Payments.First().PaidAmount)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Payments.First().PaymentRefNo)
            </th>
            <th>
                Action
            </th>
        </tr>

        @foreach (var item in Model.Payments.Select((x, i) => new { Data = x, Index = i }))
        {
            <tr>
                <td>
                    @(item.Index + 1)
                </td>
                <td>
                    @item.Data.PaymentDate.ToString("dd MMM yyyy")
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Data.PaymentMethodName)
                </td>

                <td>
                    @Html.DisplayFor(modelItem => item.Data.PaidAmount)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Data.PaymentRefNo)
                </td>

                <td></td>
            </tr>
        }
    </table>

</div>

